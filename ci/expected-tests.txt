# Expected minimum passing test count per wasm lane.
#
#   <lane>-<browser>  <minimum-passed>
#
# These are the EXACT counts each lane runs at HEAD, not a floor with slack.
# A lane that runs FEWER tests than this fails: that is the whole point, and
# it is what catches a suite that silently stopped being compiled in (see
# trap 2 in RESUME_HERE.md — a misconfigured wasm lane exits 0 having run
# zero tests). Running MORE than this is fine and never fails.
#
# WHEN YOU ADD TEST CASES YOU MUST BUMP THESE NUMBERS. Run the lane, take the
# "OK: N test(s) actually ran and passed." number, and write N here.
#
# Last verified: 2026-08-24, the 0.1.2 release pass: 157 per lane.
# (154 at racing opens on the web, +2 that landed with 0.1.1 and were never
#  written down here — proof that a floor left too low is a guard gone quiet,
#  since the lanes stayed green while the real count moved — and +1 for
#  tests/web_shell_recovery.rs, the store-less-database repair, which cannot
#  be a conformance case because the broken shell has to be built through the
#  raw IndexedDB factory.)
#
# plain-chrome, plain-firefox and atomics-chrome were each measured directly
# on this pass and all three came back 157. atomics-firefox runs the same
# cases as atomics-chrome, and Safari runs the plain suite, so all five carry
# the same number.
plain-firefox    157
plain-chrome     157
atomics-chrome   157
atomics-firefox  157
# Safari runs the same plain suite; the lane is required as of 2026-08-21.
plain-safari     157
